import { View, Text } from "react-native";
import React from "react";

const sideBarIndex = () => {
  return (
    <View>
      <Text>sideBarIndex</Text>
    </View>
  );
};

export default sideBarIndex;
